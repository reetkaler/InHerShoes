// import express from 'express';
// import dotenv from 'dotenv';
// import cors from 'cors';

// import { analyzeResponse } from './response.js';



// app.use(cors());
// dotenv.config();

// const app = express();
// app.use(express.json());

// app.post('/analyze-response', async (req, res) => {
//   const { scenario, userResponse } = req.body;
//   const feedback = await analyzeResponse(scenario, userResponse);
//   res.json({ feedback });
// });

// app.listen(3000, () => {
//   console.log('Server running on http://localhost:3000');
// });
